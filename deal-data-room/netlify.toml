[build]
  command   = "npm install && npm run build"
  publish   = ".output/public"

[build.environment]
  NODE_VERSION  = "20"
  NETLIFY       = "true"

[[redirects]]
  from   = "/*"
  to     = "/.netlify/functions/server"
  status = 200
